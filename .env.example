POSTGRES_USER=your_db_user
POSTGRES_PASSWORD=your_db_password
POSTGRES_DB=churn_prediction
DB_HOST=db
DB_PORT=5432
DB_NAME=churn_prediction
DB_USER=your_db_user
DB_PASS=your_db_password
DB_ROW_LIMIT=10000

XGB_TOTAL_TRIALS=10 #Optuna trials - More will test a wider range of hyperparameters
MLP_TOTAL_EPOCHS=50

ENVIRONMENT=docker #aws, docker or dev. Allows for dockerized or AWS ready deployment (local not tested)

BUILD_MODE=docker   # <<< when ENVIRONMENT=aws
# or BUILD_MODE=docker   # <<< when ENVIRONMENT=docker
# or BUILD_MODE=local    # <<< when ENVIRONMENT=local
